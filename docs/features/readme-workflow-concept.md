# README + концепт workflow разработки API — обозначить вектор

> Тикет: доработать `README.md` относительно того, что готово сейчас, и **обозначить вектор** проекта.
> Плюс — отдельный концепт-документ с графом детерминированного workflow разработки API (C4).

## Вектор проекта (в README отразить)

**Цель харнеса и воркфлоу:** мультиагентская разработка в **детерминированном воркфлоу** с учётом
**осей процессов**:

| Ось | Значения |
|---|---|
| **Задача** | epic · фича · улучшение · баг-хотфикс · рефакторинг |
| **Приложение** | web · mobile · cli · go-api |
| **Среды** | CI · PROD |

**Задача проекта:** проектировать и разрабатывать **распределённые приложения** на **фиксированном
стеке** по **дисциплине рациональной разработки** — научный подход к структурированию кода и
тестированию.

**Текущий статус (в README честно):** в процессе тестирования и отладки **workflow разработки API-сервиса**
(одна ось: задача=фича/модуль · приложение=go-api · среды=CI→PROD-канарейка). Остальные оси (web/mobile/cli,
epic/hotfix/рефакторинг) — вектор, не реализовано.

## Что сделать

1. **Отдельный концепт-документ** `docs/concept-api-workflow.md` — **детерминированный флоу разработки API
   нарисовать в виде графа в C4** (Mermaid, рендерится на GitHub; валидируется `validate-mermaid`).
   Граф: конвейер ролей от BRD до прод-канарейки через три Human Gate — triage → intake → slicer → usecase
   → apidesigner (frozen) → moduledesigner → ticketer → planner → mills → **Gate #1** → scaffolder →
   {wirth-tester component RED · hughes модули} → linger приёмка → **Gate #2 (мерж)** → michtom канарейка
   → **Gate #3 (приёмка)**. Показать детерминированные гейты-валидаторы (validate-frd/slices/mermaid/tickets)
   и точки LLM-решений.
2. **README.md:** обновить под текущее готовое; добавить блок «Вектор» (оси задача/приложение/среды +
   цель); добавить **ссылку на `docs/concept-api-workflow.md`** + **краткое описание workflow**.

## Критерий приёмки
- README отражает: цель, оси процессов, текущий статус (API-workflow в отладке), ссылку на концепт.
- `docs/concept-api-workflow.md` — C4/Mermaid граф API-флоу, проходит `validate-mermaid`, рендерится на GitHub.

## Связи
Артефакты: `README.md`, новый `docs/concept-api-workflow.md`. Скилл `c4` (Mermaid C4) + `validate-mermaid`.
Опирается на `docs/00_PROCESS.md`, `CONCEPT.md`, `AGENTS.md` (существующий процесс/правила).
