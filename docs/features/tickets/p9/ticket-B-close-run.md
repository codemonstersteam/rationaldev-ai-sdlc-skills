# Тикет B — close-run.mjs

**Владеет:** `harness/close-run.mjs`, `harness/test/close-run.test.mjs`.
**Не трогает:** остальное. Общий контракт §2 (`/debt/`), §3 (вайп) — соблюдать дословно.

## B1 · Вайп-инверсия (P9-2)

`WIPE` сейчас — список того, что стирать, и отстаёт (не чистит `.agent/plan-reviewer/`, `.agent/planner/{frd,
slices,target,network-topology,rollout-plan}`, `.agent/release-health/`). Переписать на **обратный принцип**:
стирать `.agent/` **целиком**, сохраняя только явный белый список. Кандидат в сохраняемое — `.agent/memory.md`
(скилл `memory`). Проверить в коде/скилле, действительно ли он должен пережить задачу; если нет — белый список
пуст, вайп = снести `.agent/` и создать заново. `/debt/` вне `.agent/` — его вайп не трогает.
Тест: полнота вайпа (ничего из `.agent/` не переживает, кроме белого списка); сейчас
`close-run.test.mjs` `plan-reviewer/` не покрывает — покрыть.

## B2 · `TAG_EXISTS` → отказ (P9-7)

`close-run.mjs:121` при уже существующем теге молча продолжает и пишет в `LEDGER.md` с чужим мержем.
`patch-flow.md:51` обещает **отказ**. Привести к доку: тег уже есть → STOP (не перезаписывать историю версий),
внятная строка результата. Тест на этот путь.

## B3 · Археология релиза (P9-7)

После успешной постановки тега — **best-effort**: `gh release create <tag>` от тега (тело из строки причины +
ссылка на PR) и комментарий в PR «released as `<tag>`». Не легло (нет прав/`gh`) — прогон **не рушится**,
строка результата это называет (`ledgerPushed`-стиль). Тег и `LEDGER` уже стоят до этого шага.

## B4 · Удаление долга при закрытии (общий контракт §2)

Если прогон нёс маркер `.agent/planner/resolves-debt` = `task-NNN` (задача закрывала именно этот долг) и мерж
подтверждён — удалить `/debt/task-NNN.md` (исчезновение = доказательство оплаты; факт остаётся в `LEDGER`).
Маркера нет → долги не трогать. Порядок: удаление после записи в `LEDGER`, до/вместе с вайпом. Тест: с маркером
файл удалён, без маркера — цел.

## DoD

- `node --test harness/test/close-run.test.mjs` зелёный, новые кейсы на B1–B4.
- `node --test harness/test/*.mjs` зелёный (254 + новые).
- Ядро остаётся чистым (`planClose` без git/fs), побочки в CLI-хвосте — как сейчас.
