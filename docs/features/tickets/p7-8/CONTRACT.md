# P7–P8 Тоггл + актуализация документации — общий контракт

Источник — [`../../harness-hardening-and-onboarding.md`](../../harness-hardening-and-onboarding.md) §P7–P8.
Два пакета делят контракт. Часть тоггла уже закрыта в #99/#100 — **не переделывать**, только добить открытое.

## Уже сделано (проверено, НЕ трогать по-новой)
- **greenfield** — тоггла нет (`greenfield-flow.md:20`, CONCEPT). ✅
- **minor** — новая способность default OFF (было изначально). ✅
- **patch** — «тоггла нет, чинится существующее» (`patch-flow.md:18`). ✅ (добавить только оговорку, см. ниже)
- **major** — «окно совместимости» упомянуто в миграции (`major-flow.md:8`). ⚠ добить явный переключатель.

## Открытое — что реализуем

### 1. `@dijkstra` — автор README на minor/major (ГЛАВНОЕ, P8)
Сейчас `@dijkstra` запускается только в greenfield (`izi.md:253` шаг 5.5); minor/major прямо пишут «README не
запускается» (`minor-flow.md:39`). Дыра: аддитивная/ломающая правка меняет документированное поведение и «Карту
режимов отказа», а обновить README некому — `@fagan` вправе забраковать устаревший README, править нечем.
- **Триггер УЗКИЙ:** `@dijkstra` зовётся в конце minor/major **только если дельта трогает README / карту отказов**
  (новая поверхность, новый режим отказа). На чисто внутренней правке — НЕ зовётся.
- **`@dijkstra` change-mode:** обновляет существующий README из `change-delta` (не полный greenfield-прогон
  README) — правит затронутые разделы + «## Карта режимов отказа». Единоличный автор README сохранён.
- **`@wirth-ticketer`** README-тикет по-прежнему НЕ режет (единоличность). Добавить пометку: автор — `@dijkstra`.
- **`@fagan`** проверяет `validate-readme` + что README отражает изменение (не устарел).

### 2. Сведение дизайн-пакета `<change-dir>` → срез (P8, owner = допущение оператора можно поправить)
После мержа изменения из `<change-dir>/{module-tree,contracts,c4}.md` должны попасть в канонический дизайн-пакет
среза `docs/design/<slice>/`, иначе копится дрейф (следующая задача читает устаревшую карту).
- **Owner по умолчанию — `@wirth-moduledesigner`** как под-шаг приёмки (он владеет дизайн-пакетом): сводит
  `<change-dir>` design → срез перед закрытием прогона. Помечено как допущение — оператор может переназначить.
- Применимо к minor/major (там дизайн менялся); patch с `design=needed` — тоже.

### 3. patch — оговорка про рискованный фикс (P7, мелочь)
В `patch-flow.md` к «тоггла нет» добавить: **рискованный фикс за тогглом — опция оператора** (не правило).

### 4. major — переключатель совместимости в ADR (P7, добить)
В major: решение о переключателе совместимости / миграционном окне фиксируется в **ADR** и проверяется на
приёмке `@fagan`. Сделать явным (сейчас только «окно совместимости» в прозе миграции).

## Границы пакетов (не пересекаться)

| Пакет | Владеет |
|---|---|
| **A — роли + проводка** | `harness/agents/_shared/{izi,dijkstra,wirth-ticketer,fagan,wirth-moduledesigner}.md` + прогон `gen-agents.mjs` (перегенерит проекции) |
| **B — флоу-доки** | `docs/flows/{minor,major,patch}-flow.md` |

Пакет B НЕ трогает `_shared`/gen-agents; пакет A НЕ трогает `docs/flows`. Оба следуют пунктам 1–4 согласованно.
Тесты зелёные (baseline 325), роли ≤300 строк (кроме izi — предсуществующий перебор, не раздувать сверх нужды).
Якорь «секрет» не вводить (information hiding). Стиль флоу — вариант A (интро + спека-таблица), как в #100.
