# language: ru
Функция: Подключаемый мультиагентный харнес
  Как инженер, выбравший раннер (Claude / Codex / OpenCode)
  Я хочу подключить харнес одной командой и вести задачу по SDLC-ролям
  Чтобы не переключать агентов вручную и иметь трассируемость

  # --- @auto: установка детерминирована, прогоняется harness/smoke/run.sh ---

  @auto
  Сценарий: Установка для Claude кладёт роли и скиллы
    Дано чистый проект
    Когда выполняется "./install.sh claude <proj>"
    Тогда в .claude/agents 6 ролей-агентов
    И в .claude/skills есть скилл memory
    И создан CLAUDE.md

  @auto
  Сценарий: Установка для OpenCode кладёт агентов и инструкции
    Дано чистый проект
    Когда выполняется "./install.sh opencode <proj>"
    Тогда в .opencode/agent 6 агентов
    И создан корневой AGENTS.md

  @auto
  Сценарий: Установка для Codex собирает роли в AGENTS.md
    Дано чистый проект
    Когда выполняется "./install.sh codex <proj>"
    Тогда в .agents/roles 6 ролей и в .agents/skills есть memory
    И AGENTS.md содержит блок orchestrator и блоки ролей (Hughes)

  @auto
  Сценарий: Существующий AGENTS.md не затирается
    Дано в проекте уже есть AGENTS.md с содержимым оператора
    Когда выполняется "./install.sh codex <proj>"
    Тогда оригинальный AGENTS.md не изменён
    И инструкции харнеса положены рядом как AGENTS.harness.md

  # --- @manual @model: маршрутизация требует живой модели, прогоняет оператор ---

  @manual @model
  Сценарий: Модульная задача проходит planner → plan-reviewer → implementer
    Дано задача уровня «модуль» (1–2 модуля, меняется контракт)
    Когда orchestrator классифицирует уровень и делегирует
    Тогда planner (Wirth) выдаёт план и контракт
    И plan-reviewer ОТКЛОНЯЕТ план при дыре в контракте
    И после правки и Gate #1 implementer (Hughes) реализует slice
    И переходы ролей записаны в .agent/decisions.log

  # --- @manual @model: маршрутизация по РУССКИМ триггерам при английских скиллах ---
  # Проверяет, что русская фраза оператора даёт верную точку входа (скиллы грузятся
  # по имени, языко-чувствителен только роутинг orchestrator). Прогон ×N на целевой модели.

  @manual @model @routing
  Сценарий: Пустой проект — русский триггер ведёт в requirements-intake
    Дано чистый проект с установленным харнесом
    Когда оператор пишет "с чего начать?"
    Тогда orchestrator отвечает фразой о сборе бизнес-требования
    И точка входа — requirements-intake (через planner): акторы, use-cases, контракт, карта отказов

  @manual @model @routing
  Сценарий: Тривиальная правка — русский триггер ведёт в implementer
    Дано чистый проект с установленным харнесом
    Когда оператор пишет "поправь опечатку в README"
    Тогда orchestrator классифицирует уровень как тривиальный
    И точка входа — implementer напрямую (контракт не меняется)

  @manual @model @routing
  Сценарий: Модульная задача — русский триггер запускает полный маршрут
    Дано чистый проект с установленным харнесом
    Когда оператор пишет "добавь эндпоинт регистрации пользователя"
    Тогда orchestrator классифицирует уровень как модульный
    И маршрут planner → plan-reviewer → [Gate #1] → implementer → fixer

  @manual @model @routing
  Сценарий: Эпик — русский триггер ведёт в декомпозицию
    Дано чистый проект с установленным харнесом
    Когда оператор пишет "новый сервис уведомлений на три сервиса"
    Тогда orchestrator классифицирует уровень как эпик (>1 сервис)
    И идёт декомпозиция (platform-landing PART II) → per-slice планы

  @manual @model @routing
  Сценарий: Расплывчатое требование не угадывается
    Дано чистый проект с установленным харнесом
    Когда оператор пишет неоднозначную фразу без явного уровня
    Тогда orchestrator НЕ угадывает уровень
    И просит оператора уточнить или описать бизнес-требование (STOP)
