# Скопируй в bench/proxy/.env и заполни. .env в .gitignore — реальные ключи не коммить.
ANTHROPIC_API_KEY=sk-ant-...
LITELLM_MASTER_KEY=sk-bench-local
# baseURL, который харнесы (OpenCode) должны использовать вместо прямого провайдера:
#   OPENAI_BASE_URL / ANTHROPIC_BASE_URL → http://localhost:4000
