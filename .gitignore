# Env
.env
.env.local

# Рабочее состояние харнес-прогона (izi) — run-артефакт, не коммитить
.agent/

# Build artifacts
/bin/
/dist/
*.exe

# Go
vendor/

# IDE
.idea/
.vscode/
*.swp

# OS
.DS_Store
Thumbs.db

# SQLite
*.db
*.sqlite

# Сырые прогоны token-bench (raw dumps, могут нести секреты) — не коммитить (RUNBOOK §Save)
experiments/token-bench/baseline-run*/
**/flow.jsonl
**/usage.jsonl

# Референс-PDF (внешние материалы, не часть репо)
docs/references/*.pdf
